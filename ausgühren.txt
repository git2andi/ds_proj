cd C:\Users\andi\Desktop\ds_proj\
git add .
git commit -m "v14_validate"
git push

Die 4 Zeilen gemeinsam auswählen, strg+c und dann rechtsklick in das feld unten; dann enter.
wenn alles durch ist einfach schließen / runterfahren.

All 12 items of docs/todo_validate.md are complete. Final state verified: 527 deterministic tests OK, all compiles clean, and a full live 10-case suite with zero unsupported/invalid printed turns, zero blocker violations, zero vote/state inconsistencies, and zero controller-language leaks across 355 turns.

Summary

I worked through docs/todo_validate.md strictly, item by item, with focused tests and real-pipeline traces before advancing. Full audit trail in docs/validate_baseline.md.

Correctness fixes (items 2–5) — the substance, all verified live:
- Questions: replaced the pronoun-specific catalog with a small grammatical detector (WH/aux clause + choice + tag-tail exclusion). The three real ASK_NOT_REALIZED defect lines now open threads cleanly — 0 printed ASK_NOT_REALIZED across the suite.
- Comparisons: added a deterministic visible_comparison merge; COMPARISON_MISSES_OPTIONS is now reserved for genuine wrong-pair/single-option generations.
- Fallback: rewrote the vote/blocker fallback to be minimal and public — a switch is stated only from a prior public commitment, with no intent.allowed_reason/private-preference leakage. 0 controller-language leaks live.
- Grounding: unified opinion/inference/uncertainty so qualified inferences pass (killing the 125 over-rejections) while invented numbers and card-contradicting values still fail. The coffee grounding case went from worst-in-baseline (0.38 repair) to clean.

Structural (items 6–8): confirmed the validator pipeline was already single-call/bounded-retry/intent-schema/repair-on-blocking-only, added a guarded COMPARE fast path; removed the last duplicated semantic representation (DialogueAct); consolidated redundant test modules.

Eval (items 9–12): rebuilt the suite to exactly 10 matrix cases with case_id everywhere, restart-safe orphan cleanup, per-case acceptance flags, a leak detector, and split validator API-vs-logical metrics.

Honest caveats (reported, not papered over): the three reduction targets — validator cost (~flat: 1.04 API calls/turn vs 1.04 baseline), production LOC, and unit-test count — were not met, all for the same reason the TODO's own escape clauses anticipated: the soft SUPPORT/CONCERN discussion turns inherently require the validator, and the items 2–6 correctness code offsets deletions. Aggregate repair 0.169 (marginally over 0.15); one case (c06, n=5 auto) at 0.37 with all repairs succeeding. These are documented in validate_baseline.md for the next cross-cutting pass rather than patched with another validation layer.

One config fix was needed mid-run (a clipped manual short_name), after which all 10 cases completed.
