# reasoning/ — Fisher-aligned phase detection and StanceTable-based consensus (Stage 9+)
# phase_detector.py   : Fisher evidence ratios → PhaseEvidence + confidence
# consensus_engine.py : StanceTable → ConsensusState + best_available_decision()
